# Handles No Internet popup if it appears
appId: com.kodak.steptouch
---
- runFlow:
    when:
      visible: "No Internet Connection"
    commands:
      - tapOn: "Ok"
      - waitForAnimationToEnd:
          timeout: 2000
