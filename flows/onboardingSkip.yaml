appId: com.kodak.steptouch
---
- repeat:
    times: 3
    commands:
      - runFlow:
          when:
            visible: "Skip"
          commands:
            - tapOn: "Skip"
            - waitForAnimationToEnd:
              timeout: 2000
