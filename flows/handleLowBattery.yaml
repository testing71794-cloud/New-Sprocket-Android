# Handles Low Battery popup during print flow
appId: com.hp.impulse.sprocket
---
- runFlow:
    when:
      visible: "Low Battery"
    commands:
      - tapOn: "Ok"
      - waitForAnimationToEnd:
          timeout: 2000
