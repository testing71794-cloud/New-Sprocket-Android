appId: com.hp.impulse.sprocket
---
# Select 3 photos → Next → reorder.
- runFlow: select_three_photos_fast.yaml
- tapOn: "Next"
- extendedWaitUntil:
    visible: "Drag & Drop to Reorder"
    timeout: 8000
- assertVisible: "Drag & Drop to Reorder"
- assertVisible: "Layout"
- assertVisible: "Next"
