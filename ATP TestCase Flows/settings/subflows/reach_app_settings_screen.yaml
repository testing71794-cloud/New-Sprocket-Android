# Create hub → More → Hamburger menu → App Settings (screen 3).
appId: com.hp.impulse.sprocket
---
- runFlow: reach_hamburger_menu.yaml
- tapOn: ".*(?i)App Settings.*"
- waitForAnimationToEnd:
    timeout: 3000
- runFlow: assert_app_settings_screen.yaml
