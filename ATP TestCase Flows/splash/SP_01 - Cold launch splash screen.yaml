# ATP Test Case ID: SP_01
# Cold launch — splash may be brief; first durable screen is Sign up (carousel optional).
appId: com.hp.impulse.sprocket
name: "SP_01 - Cold launch splash screen"
---
- launchApp:
    clearState: true
- runFlow: ../common/subflows/wait_after_cold_launch.yaml
- runFlow: ../common/subflows/complete_sprocket_carousel_if_visible.yaml
- extendedWaitUntil:
    visible: "Sign up"
    timeout: 20000
- assertVisible: "Sign up"
- assertVisible: ".*(?i)(I.ll do it later|Sign Up Later).*"
