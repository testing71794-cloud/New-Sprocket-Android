appId: com.hp.impulse.sprocket
---
# Photos permission — Don't allow (denied / empty-state path).
- runFlow:
    when:
      visible: ".*photos and videos.*"
    commands:
      - tapOn: "Don't allow"
      - waitForAnimationToEnd:
          timeout: 3000
